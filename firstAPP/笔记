Ajax 学习
对话框添加，删除，修改
这里我们在编辑学生的时候，我们这里应该弹出来一个对话框，我们在对话框内输入相关信息
这里我们在输入的时候，这里我们应该有正确判断，错误有提示，正确的话就提交。但是这个都应该是悄悄的在后端执行，
然后把结果返还给前端，但是页面不能刷新，因为一刷新，对话框就没有了。（所以这里我们不能通过form表单的形式提交）
    所以这里我们引入Ajax,偷偷向后台发送请求
    1、下载引入JQuery
    2、格式：
        $.ajax({
            url:'login.html',                               提交路径
            type:'POST',                                    请求方式
            data:{'username':'root','password':'123'}，     发送数据
            success:function(arg){
                //回调函数，后台返回success后执行该函数，arg是服务端返回的“字符串”
            }
        })
    3
        我们在select的时候我们代码选择的话用

        $('#sel').val(2)         单选
        $('#sel').val([1,3,5])   多选，同时在select标签添加multiple

知识回顾：
Models操作
    类：代表数据库的表
    字段：代表数据库的列
    对象：代表一行数据
一对多
    外键：代表关联表的一行数据，通过双下划线__跨表查询
多对多
    ManayToManayField字段可以放在任何一张表（两个主表和一个第三张表）
    对第三张表操作的时候只有通过间接操作，因为没有第三张表的类，所以我们只有通过主表的M2M字段来间接操作第三张表
.all() ---->queryset   里面是多个对象[obj]
.values       [{},{},{}...]
.values_list    [(),()...]

